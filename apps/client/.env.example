# Base API: si vide, utilise le proxy Vite vers 3000 en dev
VITE_SERVER_BASE_URL=http://localhost:3000
# Base publique pour ouvrir la page /p/<slug> (fallback: SERVER_BASE_URL puis http://localhost:3000)
VITE_PUBLIC_BASE_URL=
VITE_EVENT_ID=default-event
# Optionnel: URL d'un PNG overlay à dessiner sur la photo en client
VITE_OVERLAY_URL=
